As I mentioned in the top DIR_ALT_README, don't pay too close attention to the files or folders
ending with .placeholder, but since here it might be confusing I will clarify that canvas.tscn
would be a CanvasItem (so 2D or Control) node in the shape of a rectangle that treats input as
a drawing and displays it as it is drawn. This is node need not exist, thus it is a placeholder,
but that's why it needs to be a scene node in the drawing folder. I guess it could be a single
script.

Point is, canvas.tscn a placeholder to show that more logic-oriented folders like drawing can
have scenes too.
